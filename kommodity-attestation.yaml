name: kommodity-attestation
container:
  entrypoint: ./kommodity-attestation-extension
configuration: true
depends:
  - path: /dev/tpm0
  - network:
      - addresses
      - connectivity
      - hostname
      - etcfiles
restart: once
